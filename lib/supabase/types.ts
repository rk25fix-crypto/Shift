/**
 * Placeholder for the generated Supabase types.
 *
 * Once a real Supabase project exists, regenerate this file with:
 *   supabase gen types typescript --project-id <project-ref> > lib/supabase/types.ts
 *
 * Until then, `Database` is left untyped so the app still compiles.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
